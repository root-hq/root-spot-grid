export const MARKET_SEED = Buffer.from("market");
export const TRADE_MANAGER_SEED = Buffer.from("trade-manager");
export const BASE_TOKEN_VAULT_SEED = Buffer.from("base-token-vault");
export const QUOTE_TOKEN_VAULT_SEED = Buffer.from("quote-token-vault");
export const POSITION_SEED = Buffer.from("position");